# telegram-bot-company
create a basic bot for ccacc.

This bot does two things:
1. Welcome message with image + text when someone joins
2. Username check — prompts new members to set a username if they don't have one
